module.exports = {
  theme: {
    // Overrides of theme default theme
    fontFamily: {
      body: "Inter",
    },
    colors: {
      transparent: "transparent",
      white: "#FFFFFF",
      gray: {
        dark: "#1A1C1D",
        light: "#98A3AA",
        lighter: "#d3dade",
      },
      green: "#05C77E",
    },

    // Extensions of theme default theme
    extend: {
      borderWidth: {
        "3": "3px",
      },
    },
  },
}
